const operations = {
    celsiusToFahrenheit: (celsiusDegree) => {
        return (celsiusDegree * 9/5) + 32;
    },
    fahrenheitToCelsius: (fahrenheitDegree) => {
        return (fahrenheitDegree -32) * 5/9;
    }
}

export {operations};



// que vimos hoy y que tiene que estudiar para mañana

// ASINCRONIA
// asincAwait, deny catch y promesas

// que es una promesa, como funciona, EJERCICIO HACERLO LLAMANDO A UN API
// LLEGAR CON EJERCICIOS HECHOS O QUE INTENTAMOS HACER